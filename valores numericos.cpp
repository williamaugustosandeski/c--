#include <stdio.h>
int main(){
	int numeros;
	int soma;
	do{
		printf("Digite um numero : ");
		scanf("%d", &numeros);
		
		if (numeros >=0 );
		
			soma = soma + numeros;
		
		}while( numeros != 0 );
			printf("A soma e %d\n", soma);
return 0;	
	}